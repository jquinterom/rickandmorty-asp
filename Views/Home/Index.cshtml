@{
  ViewData["Title"] = "Character List";
}

@model ApiResponse<Character>

<h1>Personajes de Rick and Morty</h1>

<div id="character-container">
  @await Html.PartialAsync("_CharacterListPartial", Model.Results)
</div>

@await Html.PartialAsync("_CharacterPagination", Model.Info)
